# Review 2 handoff

## Outcome

The repair closes every finding in `.factory/review-2.md`, `.factory/review-1.md`, and the earlier verification records. The exact finding map is `.factory/polish-2.md`.

The web demo transcript is generated by executing the real CLI at site-build time. Its only normalized value is the random operating-system temporary folder suffix.

## Run and verify

```sh
npm ci
npm test
npm run build
cargo fmt --check
cargo clippy --all-targets -- -D warnings
./dist/bin/api-example-linter demo
```

`npm test` passed locally: 9 Rust unit tests, 7 CLI integration tests, 1 doctest, 17 static-site tests, 16 claim tests, and 6 browser/accessibility tests. The browser suite covers axe WCAG A/AA, privacy request logging, offline reload, focused route navigation, 404 behavior, metadata, and mobile target sizes.

Build output is `dist/bin/api-example-linter` and `dist/site`. `cargo package --locked` should be run after committing; Cargo intentionally rejects a dirty working tree.

## Deployment and live evidence

Deployment follows the static work order: `npm ci && npm run build:site`, publishing `dist/site` after the final push to `main`. Add the final commit, deployment result, and cold live recheck here after pushing.

## Known gaps

None. No finding is deferred.
